
/**include**/
#include <stdio.h>

/**main**/

/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */

int main(void) /**main**/
{
	int num;
	/** do stuff **/
	for (num = 0; num < 10; num++)
		printf("%d", num);

	printf("\n");

	return (0);
}
