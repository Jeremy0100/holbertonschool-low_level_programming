#include "main.h"

/**
* _abs - computes absolute value of an integer
*
* Description: Program with a prototype. Computes
*		absolute value of an integer.
*
* @n: n variable
*
* Return: Nothing
*
*/


int _abs(int n)
{
	if (n >= 0)
		return (n);
	return (-n);
}
