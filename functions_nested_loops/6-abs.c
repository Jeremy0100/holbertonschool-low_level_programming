#include "main.h"

/**
* _abs - computes absolute value of an interger
*
* Description: Program with a prototype
*
* @n: n variable
*
* Return: Nothing
*
*/


int _abs(int n)
{
	if (n >= 0)
		return (n);
	return (-n);
}
